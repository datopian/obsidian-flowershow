---
"flowershow": patch
---

Never create sites from the plugin. Publishing now requires a Flowershow site that already exists with the exact same name as the "Site Name" setting; if it doesn't exist, publishing fails with a clear error instead of creating one.
